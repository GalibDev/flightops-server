"use client";import {useForm} from "react-hook-form";import {toast} from "sonner";type Form={name:string;email:string;subject:string;message:string};export default function ContactForm(){const {register,handleSubmit,reset,formState:{errors,isSubmitting}}=useForm<Form>();async function send(v:Form){const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(v)});const j=await r.json();if(!r.ok)return toast.error(j.message);toast.success(j.message);reset()}return <form onSubmit={handleSubmit(send)} className="card p-7"><div className="grid gap-4 md:grid-cols-2"><Field label="Name" error={errors.name?.message}><input className="input" {...register("name",{required:"Name is required"})}/></Field><Field label="Email" error={errors.email?.message}><input className="input" type="email" {...register("email",{required:"Email is required"})}/></Field></div><Field label="Subject" error={errors.subject?.message}><input className="input" {...register("subject",{required:"Subject is required"})}/></Field><Field label="Message" error={errors.message?.message}><textarea className="input min-h-36" {...register("message",{required:"Message is required",minLength:{value:10,message:"Please add a little more detail"}})}/></Field><button disabled={isSubmitting} className="btn btn-primary w-full">{isSubmitting?"Sending…":"Send message"}</button></form>}function Field({label,error,children}:{label:string;error?:string;children:React.ReactNode}){return <label className="mb-4 block text-sm font-bold">{label}<div className="mt-2">{children}</div>{error&&<small className="text-red-600">{error}</small>}</label>}
