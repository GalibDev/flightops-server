"use client";import {useRouter} from "next/navigation";import {Search} from "lucide-react";import {useState} from "react";
export default function FlightSearch(){const r=useRouter();const [from,setFrom]=useState("Dhaka");const [to,setTo]=useState("");const [date,setDate]=useState("");function go(){const q=new URLSearchParams({from,to,date});r.push(`/flights?${q}`)}return <div className="card grid gap-3 p-4 md:grid-cols-[1fr_1fr_1fr_auto]"><label className="text-xs font-bold text-slate-500">FROM<input className="input mt-2" value={from} onChange={e=>setFrom(e.target.value)} placeholder="Departure city"/></label><label className="text-xs font-bold text-slate-500">TO<input className="input mt-2" value={to} onChange={e=>setTo(e.target.value)} placeholder="Where to?"/></label><label className="text-xs font-bold text-slate-500">DEPARTURE<input className="input mt-2" type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><button onClick={go} className="btn btn-primary self-end"><Search size={18}/>Search</button></div>}
