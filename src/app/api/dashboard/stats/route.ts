import {NextResponse} from "next/server";import {flights} from "@/lib/flights";export async function GET(){return NextResponse.json({success:true,message:"Statistics loaded",data:{totalFlights:flights.length,activeFlights:flights.filter(f=>f.status==="active").length,totalBookings:12543,totalRevenue:2450000,totalPassengers:8743,availableSeats:flights.reduce((a,b)=>a+b.availableSeats,0)}})}
