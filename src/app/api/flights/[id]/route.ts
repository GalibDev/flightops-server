import {NextResponse} from "next/server";import {flights} from "@/lib/flights";import {currentUser} from "@/lib/auth";import {connectDB} from "@/lib/db";import {FlightModel} from "@/models/Flight";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const f=flights.find(x=>x.id===id);return f?NextResponse.json({success:true,message:"Flight loaded",data:f}):NextResponse.json({success:false,message:"Flight not found"},{status:404})}export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const user=await currentUser();if(!user)return NextResponse.json({success:false,message:"Authentication required"},{status:401});const {id}=await params;if(await connectDB())await FlightModel.findOneAndDelete({_id:id,...(user.role==="admin"?{}:{createdBy:user.id})});return NextResponse.json({success:true,message:"Flight removed"})}
