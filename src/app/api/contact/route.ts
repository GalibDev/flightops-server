import {NextResponse} from "next/server";import {z} from "zod";const schema=z.object({name:z.string().min(2),email:z.string().email(),subject:z.string().min(3),message:z.string().min(10)});export async function POST(req:Request){try{schema.parse(await req.json());return NextResponse.json({success:true,message:"Thanks—your message has been received."})}catch{return NextResponse.json({success:false,message:"Please complete every field correctly."},{status:400})}}
