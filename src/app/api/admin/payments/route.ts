import {NextResponse} from "next/server";import {requireAdmin} from "@/lib/admin";import {PaymentModel} from "@/models/Payment";
export async function GET(){if(!await requireAdmin())return NextResponse.json({success:false,message:"Admin access required"},{status:403});const data=await PaymentModel.find().sort({createdAt:-1}).lean();return NextResponse.json({success:true,message:"Payments loaded",data})}
