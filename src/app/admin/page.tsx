import {currentUser} from "@/lib/auth";import {redirect} from "next/navigation";import AdminConsole from "@/components/admin/AdminConsole";export const metadata={title:"Admin Center"};export default async function AdminPage(){const user=await currentUser();if(!user)redirect("/login?next=/admin");if(user.role!=="admin")redirect("/dashboard");return <div className="min-h-screen bg-slate-50 py-10"><div className="container"><div className="mb-8"><span className="eyebrow">ADMINISTRATION</span><h1 className="section-title">FlightOps command center</h1><p className="muted">Control users, approvals, bookings, payments and accountability from one workspace.</p></div><AdminConsole/></div></div>}
