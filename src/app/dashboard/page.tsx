/* eslint-disable react/jsx-key */
import {currentUser} from "@/lib/auth";import Charts from "@/components/dashboard/Charts";import {flights} from "@/lib/flights";import {CircleDollarSign,Plane,TicketCheck,UsersRound,Armchair,Activity} from "lucide-react";
export const metadata={title:"Operations dashboard"};export default async function Dashboard(){const user=await currentUser();const stats=[[Plane,"Total flights",flights.length,"+8.2%"],[Activity,"Active flights",flights.filter(f=>f.status==="active").length,"On schedule"],[TicketCheck,"Total bookings","12,543","+12.5%"],[CircleDollarSign,"Total revenue","$2.45M","+15.8%"],[UsersRound,"Passengers","8,743","+8.2%"],[Armchair,"Available seats",flights.reduce((a,b)=>a+b.availableSeats,0),"Live capacity"]] as const;return <div className="bg-slate-50 py-10"><div className="container"><div className="mb-8"><span className="eyebrow">OPERATIONS CENTER</span><h1 className="section-title">Welcome back, {user?.name}</h1><p className="muted">A clear view of your schedules, bookings and performance.</p></div><div className="mb-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-6">{stats.map(([Icon,n,v,c])=><div className="card p-4 lg:col-span-2" key={n}><div className="flex justify-between"><span className="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon size={19}/></span><small className="text-emerald-600">{c}</small></div><div className="mt-5 text-2xl font-black">{v}</div><div className="text-sm text-slate-500">{n}</div></div>)}</div><Charts/><div className="card mt-5 overflow-hidden p-5"><h3 className="mb-4 font-black">Recent flight activity</h3><div className="overflow-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{["Flight","Route","Departure","Class","Status"].map(x=><th className="p-3" key={x}>{x}</th>)}</tr></thead><tbody>{flights.slice(0,5).map(f=><tr className="border-t"><td className="p-3 font-bold">{f.flightNumber}</td><td className="p-3">{f.departureAirport} → {f.destinationAirport}</td><td className="p-3">{f.departureDate}, {f.departureTime}</td><td className="p-3">{f.travelClass}</td><td className="p-3"><span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">{f.status}</span></td></tr>)}</tbody></table></div></div></div></div>}
